name: 300 Appium Mobile Automated Tests Execution

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  appium-mobile-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Codebase
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: mobile/package-lock.json

      - name: Install Google Chrome Headless
        run: |
          sudo apt-get update
          sudo apt-get install -y wget curl unzip libxi6 libnss3
          wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
          sudo sh -c 'echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google-chrome.list'
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable

      - name: Install Web App Dependencies
        run: |
          cd web
          npm ci || npm install

      - name: Start Vite Web App Server on Port 8081
        run: |
          cd web
          npm run dev -- --port 8081 --host &
          echo "Waiting for web dev server on port 8081..."
          until curl -s http://localhost:8081 > /dev/null; do
            sleep 2
          done
          echo "Web server is ready on http://localhost:8081"

      - name: Install Mobile Dependencies
        run: |
          cd mobile
          npm ci || npm install

      - name: Install Appium Test Runner Dependencies
        run: |
          cd collabroom-tests/appium
          npm ci || npm install

      - name: Execute 300 Mobile Appium Test Cases
        run: |
          cd collabroom-tests/appium
          npm test

      - name: Upload Appium Excel Execution Report Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: appium-test-report
          path: collabroom-tests/reports/appium-report.xlsx
          retention-days: 14
